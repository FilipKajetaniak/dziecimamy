mutation AddCategory($id: ID!, $name: String!, $slug: String!, $color: String!, $description: String, $postsIds: [ID!]){
    updateCategory(
    id: $id
    name: $name
    slug: $slug
    color: $color
    description: $description
    postsIds: $postsIds){
        id
    }
}